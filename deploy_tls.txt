apiVersion: apps/v1
kind: Deployment
metadata:
  name: helloweb
spec:
  replicas: 3
  selector:
    matchLabels:
      app: hello
      tier: web
  template:
    metadata:
      labels:
        app: hello
        tier: web
    spec:
      containers:
      - name: hello-app
        image: gcr.io/google-samples/hello-app-tls:1.0
        imagepullpolicy: Always
        ports:
        - containerPort: 8443
        volumeMounts:
	- name: tls